<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Photo access for the Screenshot Scanner.

        On Android 13+ (API 33+), `image_picker` opens the system Photo
        Picker which doesn't require a runtime permission — these
        declarations are only honoured on the SDK versions below.

        `maxSdkVersion` keeps each line self-removing once it's no
        longer needed, so the manifest stays minimal on newer devices.
    -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        android:minSdkVersion="33" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
        Camera access for the QR Checker.

        mobile_scanner requests this at runtime when the user taps
        the QR tile, so the static declaration is enough — no
        `maxSdkVersion` gymnastics here. `required="false"` keeps the
        APK installable on devices without a camera (the QR Checker
        still works because we offer a "Enter QR text manually"
        fallback for tablets / cameras-blocked devices).
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:label="NirapodClick"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!--
                Share-target: receive plain-text shares from other apps
                (SMS, WhatsApp, Messenger, browser, etc.) and route them
                into the existing Message Checker. The `share_handler`
                plugin (lib/services/share_intent_service.dart) catches
                the ACTION_SEND intent on the platform side and exposes
                the text via `getInitialSharedMedia` (cold start) and
                `sharedMediaStream` (warm start).

                We deliberately scope this to text/plain only — image
                / file / multi-attachment shares fall through to the
                system default behaviour because the NirapodClick
                analysis pipeline is text-only.
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
